[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
dockerBuildArgs = ["VITE_API_KEY"]

[deploy]
startCommand = "python start.py"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
